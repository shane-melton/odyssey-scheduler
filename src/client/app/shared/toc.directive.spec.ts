import { TocDirective } from './toc.directive';

describe('TocDirective', () => {
  it('should create an instance', () => {
    const directive = new TocDirective();
    expect(directive).toBeTruthy();
  });
});
