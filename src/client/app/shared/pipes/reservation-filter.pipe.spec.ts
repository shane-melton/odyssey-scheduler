import { ReservationFilterPipe } from './reservation-filter.pipe';

describe('ReservationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReservationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
